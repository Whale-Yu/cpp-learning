// 预处理指令：代码编译前的准备工作
#include "iostream"
using namespace std;

// 主函数（程序入口）
int main() {

    // 逻辑代码（具体的功能代码），每一行都要以;结尾
    // cout=对外输出，"xxx"=输出的字符串   endl=换行符
    cout << "Hello, World!" << endl;

    // 返回值语句，固定写法
    return 0;
}